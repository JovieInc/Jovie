# TODO - Admin menu + refresh UX
- [ ] Style CreatorActions dropdown items with sidebar nav states (use sidebar tokens / data-highlighted classes).
- [ ] Refresh action: use refresh icon; on click, show avatar loader for that row; update DOM after success (refetch or optimistic update).
- [ ] Smooth row insertion for pending ingests (transitions/animate from placeholder to final row).
- [ ] Fix class lint warnings (data-highlighted syntax) and rerun eslint.
