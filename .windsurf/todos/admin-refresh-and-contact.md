# TODO - Admin creator refresh + contact sync + table layout
- [ ] Add per-creator “Refresh ingest” action in admin creators table (per-row button with loading/toast) calling Linktree ingest for that handle.
- [ ] Align bulk actions control above username column; keep select-all in header, remove redundant top bar.
- [ ] Render creator name + username as two lines: bold displayName (or username), smaller username below.
- [ ] Contact sidebar: load creator data (display name, username, avatar, social links) so it stays in sync.
- [ ] After ingest, refresh row/contact sidebar data to show updated socials/avatar/name.
- [ ] Run lint/tests if touched files require.
