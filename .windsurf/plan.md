# Plan
1. Find where handle/username updates are saved (settings/profile actions or API).
2. Find where “View my profile” URL is built in dashboard/user menu.
3. Update flow to use fresh handle after save; verify redirect uses new handle.
