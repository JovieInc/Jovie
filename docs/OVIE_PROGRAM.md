# Ovie → Eve → Summer → Symphony/Gem

**Program parent:** JOV-5214  
**Founder correction:** 2026-08-19/20  
**Status:** Canonical. Supersedes conflicting historical issue prose, docs, configs, agent output, and PR descriptions.

**Runtime status (2026-09-02):** Hermes and Trigger.dev are retired. The
recoverable local Summer profile is offline. Eve is the approved durable target,
but no Eve-backed Summer conversation is commissioned until the identity,
deployment, persistence, privacy, failure, and recurrence receipts pass.

Machine-checked contract: `apps/web/lib/ovie/program.ts`.

## Canonical flow

`Tim -> Ovie -> Eve intake/ack -> durable Kanban -> Summer -> Symphony -> identified coding worker on Gem Ubuntu`

| Role | Is | Is not |
|---|---|---|
| **Ovie** | Tim's macOS/iOS door and operations presentation | Agent persona, model identity, independent memory, source of truth |
| **Eve** | Durable persist, classify, ack, route | Discretionary authority, Summer answers, code dispatch, self-promotion, permission broadening |
| **Summer** | Chief of Staff: priorities, decomposition, follow-up, audits, routing, company Kanban | Product-code editor. Personal work never enters this Kanban or Linear |
| **Symphony** | Sole coding orchestrator | A host or persona |
| **Gem** | Ubuntu execution host | An agent or persona |

Coding provenance names `orchestrator=symphony`, `execution_host=gem`, and the identified worker/run. GitHub's native merge queue remains merge authority. Summer's conversation path is Eve-only; source integration does not commission it. It remains unavailable until the exact deployment, identity, persistence, failure, and recurrence receipts pass.

## First milestone (M1)

Tim opens the real verified local Ovie Mac door, holds a complete multi-turn streamed conversation with the real current Summer, and sees a deliberately changed authoritative Ubuntu shipping state within ten seconds with truthful freshness, stale, disconnected, unavailable, unauthorized, degraded, unknown, failure, and recovery behavior.

Report `M1 PASSED` only after packaged/runtime dogfood and independent reproduction. Source, unit tests, endpoint health, PR, CI, native queue, merge, or deployment alone are progress, not M1.

Current contract status: **not-passed**.

## Blocking children

- JOV-5215 — Eve durable intake/ack and Summer Kanban handoff
- JOV-5212 — packaged Mac Ovie conversation with the real current Summer
- JOV-5226 — one authoritative Summer company Kanban
- JOV-5248 — Ubuntu Operational Truth normalized read-only publisher (`ovie.shipping-state.v1`, `GET /api/hud/shipping-state`)
- JOV-5249 — Ovie Operational Truth client/freshness UX (blocked by JOV-5248)
- JOV-4320 — Jovie/Summer identity, memory, credential, permission, session, and audit isolation
- JOV-5253 — restore the deployed Symphony reconciler/runtime receipt and enforce terminal exit-78 before M1 code admission

## Program gates

- The former Gem OpenClaw agent remains retired. No code path may revive or continue it.
- Ovie/Eve must never fall through to ordinary artist Jovie chat or make a model self-identify as Ovie.
- The telemetry bridge is read-only and exposes no raw logs, secrets, arbitrary command, dispatch, retry, cancel, restart, or other actuation surface.
- Proof tiers stay separate: planned, dispatched, source, tests, coverage, runtime, PR, CI, native queue, merged, deployed, exact-build, dogfood, recurrence.
- PRs #16253 and #16268 stay held. They treated Ovie as a persona/identity-pack owner. Reuse useful UI mechanics only through a corrected Symphony-owned work item; do not merge either unchanged.

HUD at `/hud` is one Ovie presentation surface, not the whole door and not a persona.
