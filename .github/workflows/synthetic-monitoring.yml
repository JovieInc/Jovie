name: Production Synthetic Monitoring

permissions:
  contents: read

on:
  schedule:
    # Run every 15 minutes during business hours (8 AM - 8 PM PST = 16:00-04:00 UTC)
    - cron: '*/15 16-23 * * *'
    - cron: '*/15 0-4 * * *'
    # Run every 30 minutes during off-hours (4 AM - 4 PM PST = 05:00-15:00 UTC)
    - cron: '0,30 5-15 * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  synthetic-test:
    name: Golden Path Synthetic Test
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Safety check - only run on schedule or manual dispatch
        if: ${{ github.event_name != 'schedule' && github.event_name != 'workflow_dispatch' }}
        run: |
          echo "❌ Synthetic monitoring should only run on schedule or manual dispatch, not on '${{ github.event_name }}'"
          echo "This workflow was incorrectly triggered. Exiting to prevent unnecessary resource usage."
          exit 1

      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Node and pnpm
        uses: ./.github/actions/setup-node-pnpm

      - name: Cache Playwright Browsers
        uses: actions/cache@668228422ae6a00e4ad889ee87cd7109ec5666a7 # v5
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('pnpm-lock.yaml', 'playwright.config*.ts') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright Browsers (chromium only)
        run: pnpm --filter=@jovie/web exec playwright install --with-deps chromium

      - name: Install Doppler CLI
        run: |
          sudo apt-get update && sudo apt-get install -y apt-transport-https ca-certificates curl gnupg
          curl -sLf --retry 3 --tlsv1.2 --proto "=https" 'https://packages.doppler.com/public/cli/gpg.DE2A7741A397C129.key' | sudo gpg --dearmor -o /usr/share/keyrings/doppler-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/doppler-archive-keyring.gpg] https://packages.doppler.com/public/cli/deb/debian any-version main" | sudo tee /etc/apt/sources.list.d/doppler-cli.list
          sudo apt-get update && sudo apt-get install -y doppler
          doppler --version

      - name: Run Golden Path Test
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_PRD }}
          BASE_URL: https://jov.ie
          PLAYWRIGHT_TEST_BASE_URL: https://jov.ie
          E2E_SKIP_WEB_SERVER: '1'
          E2E_SYNTHETIC_MODE: 'true'
          E2E_ENVIRONMENT: production
          ALLOW_EMPTY_SYNTHETIC_SUITE: 'true'
          PLAYWRIGHT_JSON_OUTPUT_NAME: test-results/results.json
        run: |
          # Temporarily skip the signup->subscription path in synthetic runs.
          # It is flaky in production due intermittent CAPTCHA bootstrap failures.
          doppler run -- pnpm --filter=@jovie/web exec playwright test tests/e2e/golden-path.spec.ts --project=chromium --reporter=line,json --output=test-results --grep-invert="complete user journey from signup to paid subscription"
        continue-on-error: true

      - name: Cleanup Synthetic E2E Users
        if: always()
        continue-on-error: true
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_PRD }}
        run: |
          cd apps/web
          doppler run -- pnpm tsx scripts/cleanup-e2e-users.ts --force

      - name: Parse test results
        id: test-results
        run: |
          node <<'NODE'
          const fs = require('node:fs');

          const resultsFile = 'apps/web/test-results/results.json';
          const outputFile = process.env.GITHUB_OUTPUT;
          const allowEmptySuite =
            process.env.ALLOW_EMPTY_SYNTHETIC_SUITE === 'true';

          function emit(line) {
            if (outputFile) {
              fs.appendFileSync(outputFile, `${line}\n`);
            } else {
              console.log(line);
            }
          }

          function emitMultiline(name, lines) {
            emit(`${name}<<EOF`);
            for (const line of lines) emit(line);
            emit('EOF');
          }

          if (!fs.existsSync(resultsFile)) {
            emit('test_status=error');
            emitMultiline('failed_tests', ['Test results file not found']);
            process.exit(0);
          }

          const results = JSON.parse(fs.readFileSync(resultsFile, 'utf8'));
          const specs = [];

          function collectSpecs(suites = []) {
            for (const suite of suites) {
              specs.push(...(suite.specs ?? []));
              collectSpecs(suite.suites ?? []);
            }
          }

          collectSpecs(results.suites ?? []);

          const tests = specs.flatMap(spec =>
            (spec.tests ?? []).map(testCase => ({ spec, testCase }))
          );
          const skipped = tests.filter(({ testCase }) =>
            (testCase.results ?? []).some(result => result.status === 'skipped')
          );
          const flaky = tests.filter(({ testCase }) => testCase.status === 'flaky');
          const failed = tests.filter(
            ({ testCase }) => testCase.status === 'unexpected'
          );
          const passed = tests.filter(
            ({ testCase }) =>
              ['expected', 'flaky'].includes(testCase.status) &&
              !(testCase.results ?? []).some(result => result.status === 'skipped')
          );

          emit(`total_tests=${tests.length}`);
          emit(`passed_tests=${passed.length}`);
          emit(`flaky_tests=${flaky.length}`);
          emit(`skipped_tests=${skipped.length}`);

          const details = [
            ...failed.map(({ spec }) => spec.title),
            ...(skipped.length > 0
              ? ['Skipped tests:', ...skipped.map(({ spec }) => spec.title)]
              : []),
          ];
          emitMultiline('failed_tests', details);

          if (tests.length === 0) {
            if (allowEmptySuite) {
              emit('test_status=passed');
            } else {
              emit('test_status=error');
            }
          } else if (failed.length > 0 || skipped.length > 0) {
            emit('test_status=failed');
          } else if (passed.length === tests.length) {
            emit('test_status=passed');
          } else {
            emit('test_status=error');
          }
          NODE

      - name: Upload test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: synthetic-test-results
          path: |
            apps/web/test-results/
            apps/web/playwright-report/
          retention-days: 30

      - name: Fail job if tests failed
        if: steps.test-results.outputs.test_status != 'passed'
        run: |
          echo "Marking job as failed because synthetic status is '${{ steps.test-results.outputs.test_status }}'"
          exit 1

      - name: Send Slack Alert on Failure
        if: steps.test-results.outputs.test_status != 'passed'
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#alerts-production'
          author_name: 'Synthetic Monitoring'
          custom_payload: |
            {
              "text": "🚨 Golden Path Test Failed - jov.ie",
              "attachments": [
                {
                  "color": "danger",
                  "fields": [
                    {
                      "title": "URL",
                      "value": "https://jov.ie",
                      "short": true
                    },
                    {
                      "title": "Failed Tests",
                      "value": "```\n${{ steps.test-results.outputs.failed_tests }}\n```",
                      "short": false
                    },
                    {
                      "title": "Action",
                      "value": "<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Details>",
                      "short": false
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Send Slack Success Notification (Scheduled Summary)
        if: steps.test-results.outputs.test_status == 'passed' && github.event_name == 'schedule'
        uses: 8398a7/action-slack@77eaa4f1c608a7d68b38af4e3f739dcd8cba273e # v3.19.0
        with:
          status: custom
          channel: '#monitoring'
          author_name: 'Synthetic Monitoring'
          custom_payload: |
            {
              "text": "✅ Golden Path Health Check - jov.ie",
              "attachments": [
                {
                  "color": "good",
                  "fields": [
                    {
                      "title": "URL",
                      "value": "https://jov.ie",
                      "short": true
                    },
                    {
                      "title": "Status",
                      "value": "All critical user journeys passing",
                      "short": false
                    },
                    {
                      "title": "Tests Passed",
                      "value": "${{ steps.test-results.outputs.passed_tests }}/${{ steps.test-results.outputs.total_tests }}",
                      "short": true
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
