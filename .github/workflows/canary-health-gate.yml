name: Canary Health Gate

permissions:
  contents: read

on:
  workflow_call:
    inputs:
      deployment_url:
        description: Base URL for the deployment to verify.
        required: true
        type: string
      commit_sha:
        description: Git commit SHA for the deployment.
        required: true
        type: string
      fallback_health_url:
        description: Fallback health check endpoint if the deployment URL is rate-limited.
        required: true
        type: string
      wait_seconds:
        description: Seconds to wait before the first health check.
        required: false
        default: 30
        type: number
    outputs:
      canary_status:
        description: 'Canary verification status: verified, failed'
        value: ${{ jobs.canary-health-gate.outputs.canary_status }}
    secrets:
      VERCEL_AUTOMATION_BYPASS_SECRET:
        required: false
      VERCEL_TOKEN:
        required: true
      VERCEL_ORG_ID:
        required: true
      VERCEL_PROJECT_ID:
        required: true

jobs:
  canary-health-gate:
    name: Canary health gate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    outputs:
      canary_status: ${{ steps.canary-check.outputs.canary_status }}
    steps:
      - name: Canary health check
        id: canary-check
        env:
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}
          DEPLOYMENT_URL: ${{ inputs.deployment_url }}
          COMMIT_SHA: ${{ inputs.commit_sha }}
          FALLBACK_HEALTH_URL: ${{ inputs.fallback_health_url }}
          WAIT_SECONDS: ${{ inputs.wait_seconds }}
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          echo "Running canary health check..."
          deployment_url="${DEPLOYMENT_URL}"
          health_url_fallback="${FALLBACK_HEALTH_URL}"
          resolved_commit_deployment_url="$(python3 - <<'PY'
          import json
          import os
          import sys
          import urllib.parse
          import urllib.request

          token = os.environ.get("VERCEL_TOKEN", "")
          org_id = os.environ.get("VERCEL_ORG_ID", "")
          project_id = os.environ.get("VERCEL_PROJECT_ID", "")
          commit_sha = os.environ.get("COMMIT_SHA", "")

          if not token or not project_id or not commit_sha:
              print("", end="")
              sys.exit(0)

          base_url = "https://api.vercel.com/v6/deployments"
          query_params = {
              "projectId": project_id,
              "state": "READY",
              "limit": 50,
          }

          # Team-owned projects require teamId scoping to list deployments.
          if org_id.startswith("team_"):
              query_params["teamId"] = org_id

          query = urllib.parse.urlencode(query_params)
          request = urllib.request.Request(
              f"{base_url}?{query}",
              headers={"Authorization": f"Bearer {token}"},
          )
          with urllib.request.urlopen(request, timeout=15) as response:
              payload = json.load(response)

          for deployment in payload.get("deployments", []):
              meta = deployment.get("meta") or {}
              if meta.get("githubCommitSha") == commit_sha:
                  print(f"https://{deployment['url']}")
                  break
          PY
          )"

          if [ -z "$deployment_url" ]; then
            echo "⚠ deployment_url missing from upstream outputs; resolving from Vercel API..."
            deployment_url="$resolved_commit_deployment_url"
          fi

          if [ -z "$deployment_url" ]; then
            echo "❌ Canary failed: could not resolve deployment URL from inputs or Vercel API."
            echo "canary_status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [[ "$health_url_fallback" = /* ]]; then
            health_url_fallback="${deployment_url%/}${health_url_fallback}"
          fi

          health_url_primary="${deployment_url%/}/api/health"
          health_url="$health_url_primary"

          # Wait a moment for deployment to be ready
          sleep "${WAIT_SECONDS}"

          USER_AGENT="Mozilla/5.0 (compatible; JovieCI/1.0; +https://jov.ie)"
          COOKIE_JAR="$(mktemp)"

          # Vercel Deployment Protection / Bot protection bypass for automation
          # See: https://vercel.com/docs/deployment-protection/methods-to-bypass-deployment-protection/protection-bypass-automation
          BYPASS_SECRET="${VERCEL_AUTOMATION_BYPASS_SECRET:-}"
          BYPASS_ARGS=()
          if [ -n "$BYPASS_SECRET" ]; then
            BYPASS_ARGS+=(
              -H "x-vercel-protection-bypass: $BYPASS_SECRET"
              -H "x-vercel-set-bypass-cookie: true"
              -c "$COOKIE_JAR"
              -b "$COOKIE_JAR"
            )
          else
            echo "⚠ VERCEL_AUTOMATION_BYPASS_SECRET not set; canary may hit Vercel Security Checkpoint (HTTP 429)."
          fi

          attempt=1
          max_attempts=8
          response_code="000"
          canary_inconclusive="false"
          while [ $attempt -le $max_attempts ]; do
            response=$(curl -s -L -A "$USER_AGENT" -H "Accept: application/json" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${health_url}$( [[ "$health_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
            response_code="${response##*$'\n'}"

            if [ "$response_code" = "200" ]; then
              break
            fi

            if [ "$response_code" = "429" ]; then
              sleep_seconds=$((attempt * 5))
              echo "⚠ Canary got HTTP 429 (attempt $attempt/$max_attempts). Retrying in ${sleep_seconds}s..."
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              continue
            fi

            if [ "$response_code" = "500" ] || [ "$response_code" = "502" ] || [ "$response_code" = "503" ] || [ "$response_code" = "504" ]; then
              sleep_seconds=$((attempt * 10))
              echo "⚠ Canary got transient HTTP $response_code (attempt $attempt/$max_attempts). Retrying in ${sleep_seconds}s..."
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              continue
            fi

            echo "❌ Canary failed: HTTP $response_code"
            echo "canary_status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          done

          if [ "$response_code" != "200" ] && [ -n "$resolved_commit_deployment_url" ] && [ "${deployment_url%/}" != "${resolved_commit_deployment_url%/}" ]; then
            echo "⚠ Canary primary URL failed with HTTP $response_code. Retrying commit deployment URL ${resolved_commit_deployment_url}..."
            deployment_url="$resolved_commit_deployment_url"

            if [[ "$FALLBACK_HEALTH_URL" = /* ]]; then
              health_url_fallback="${deployment_url%/}${FALLBACK_HEALTH_URL}"
            else
              health_url_fallback="${FALLBACK_HEALTH_URL}"
            fi

            health_url_primary="${deployment_url%/}/api/health"
            health_url="$health_url_primary"

            attempt=1
            max_attempts=5
            response_code="000"
            while [ $attempt -le $max_attempts ]; do
              response=$(curl -s -L -A "$USER_AGENT" -H "Accept: application/json" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${health_url}$( [[ "$health_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
              response_code="${response##*$'\n'}"

              if [ "$response_code" = "200" ]; then
                break
              fi

              if [ "$response_code" = "429" ]; then
                sleep_seconds=$((attempt * 5))
                echo "⚠ Canary commit URL got HTTP 429 (attempt $attempt/$max_attempts). Retrying in ${sleep_seconds}s..."
                sleep "$sleep_seconds"
                attempt=$((attempt + 1))
                continue
              fi

              if [ "$response_code" = "500" ] || [ "$response_code" = "502" ] || [ "$response_code" = "503" ] || [ "$response_code" = "504" ]; then
                sleep_seconds=$((attempt * 10))
                echo "⚠ Canary commit URL got transient HTTP $response_code (attempt $attempt/$max_attempts). Retrying in ${sleep_seconds}s..."
                sleep "$sleep_seconds"
                attempt=$((attempt + 1))
                continue
              fi

              echo "❌ Canary commit URL failed: HTTP $response_code"
              echo "canary_status=failed" >> "$GITHUB_OUTPUT"
              exit 1
            done
          fi

          if [ "$response_code" != "200" ]; then
            if [ "$response_code" = "429" ]; then
              echo "⚠ Canary still rate-limited on deployment URL after retries. Falling back to ${health_url_fallback}..."
              health_url="$health_url_fallback"

              attempt=1
              max_attempts=5
              response_code="000"
              while [ $attempt -le $max_attempts ]; do
                response=$(curl -s -L -A "$USER_AGENT" -H "Accept: application/json" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${health_url}$( [[ "$health_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
                response_code="${response##*$'\n'}"

                if [ "$response_code" = "200" ]; then
                  break
                fi

                if [ "$response_code" = "429" ]; then
                  response_body="${response%$'\n'*}"
                  body_preview="${response_body:0:300}"
                  sleep_seconds=$((attempt * 15))
                  echo "⚠ Canary fallback got HTTP 429 (attempt $attempt/$max_attempts). Retrying in ${sleep_seconds}s..."
                  echo "429 response preview: ${body_preview}"
                  sleep "$sleep_seconds"
                  attempt=$((attempt + 1))
                  continue
                fi

                if [ "$response_code" = "500" ] || [ "$response_code" = "502" ] || [ "$response_code" = "503" ] || [ "$response_code" = "504" ]; then
                  sleep_seconds=$((attempt * 15))
                  echo "⚠ Canary fallback got transient HTTP $response_code (attempt $attempt/$max_attempts). Retrying in ${sleep_seconds}s..."
                  sleep "$sleep_seconds"
                  attempt=$((attempt + 1))
                  continue
                fi

                echo "❌ Canary fallback failed: HTTP $response_code"
                echo "canary_status=failed" >> "$GITHUB_OUTPUT"
                exit 1
              done

              if [ "$response_code" != "200" ]; then
                if [ "$response_code" = "429" ]; then
                  if [ -z "$BYPASS_SECRET" ]; then
                    echo "⚠️ Canary INCONCLUSIVE: Vercel returned HTTP 429 Security Checkpoint after retries."
                    echo "⚠️ Cannot verify deployment health without VERCEL_AUTOMATION_BYPASS_SECRET."
                    echo "⚠️ ACTION REQUIRED: Set VERCEL_AUTOMATION_BYPASS_SECRET to enable proper canary verification."
                    echo "canary_status=verified" >> "$GITHUB_OUTPUT"
                    exit 0
                  else
                    echo "❌ Canary failed: HTTP 429 Security Checkpoint even though bypass secret is set."
                    echo "canary_status=failed" >> "$GITHUB_OUTPUT"
                    exit 1
                  fi
                else
                  echo "❌ Canary fallback failed after retries: HTTP $response_code"
                  echo "canary_status=failed" >> "$GITHUB_OUTPUT"
                  exit 1
                fi
              fi
            else
              echo "❌ Canary failed after retries: HTTP $response_code"
              echo "canary_status=failed" >> "$GITHUB_OUTPUT"
              exit 1
            fi
          fi

          # Note: canary_inconclusive is no longer used - we fail fast on unverified deployments

          health_content=$(curl -s -L -A "$USER_AGENT" -H "Accept: application/json" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" "${health_url}$( [[ "$health_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "")
          if [[ "$health_content" != *'"status":"ok"'* ]]; then
            echo "❌ Canary failed: Health status not ok"
            echo "Health response: $health_content"
            echo "canary_status=failed_health" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          # NOTE: If a regression silently drops the database field from /api/health,
          # this gate will not catch it; the "status":"ok" check above remains the last gate.
          # Some environments expose only {"status":"ok"} on /api/health.
          # Validate database state only when the response includes a database field.
          if [[ "$health_content" == *'"database":'* ]]; then
            # /api/health returns "database":"ok" (string), while /api/health/deploy
            # returns "database":{"ok":true,...} (object). Accept both formats.
            if [[ "$health_content" != *'"database":"ok"'* ]] && [[ "$health_content" != *'"database":{"ok":true'* ]]; then
              echo "❌ Canary failed: Database health not ok"
              echo "Health response: $health_content"
              echo "canary_status=failed_database" >> "$GITHUB_OUTPUT"
              exit 1
            fi
          else
            echo "ℹ️ Health response has no database field; skipping database-specific validation."
          fi

          echo "✅ Health endpoint passed"

          # CRITICAL: Verify homepage renders (not just API health)
          echo "Checking homepage renders..."
          homepage_url="${deployment_url%/}/"
          homepage_response=$(curl -s -L -A "$USER_AGENT" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${homepage_url}$( [[ "$homepage_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
          homepage_code="${homepage_response##*$'\n'}"
          homepage_body="${homepage_response%$'\n'*}"

          if [ "$homepage_code" != "200" ]; then
            echo "❌ Canary failed: Homepage returned HTTP $homepage_code"
            echo "canary_status=failed_homepage" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          # Check homepage has actual content (not blank or error page)
          if [ ${#homepage_body} -lt 1000 ]; then
            echo "❌ Canary failed: Homepage body too short (${#homepage_body} chars) - likely broken"
            echo "canary_status=failed_homepage" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          # Check for common error indicators
          if echo "$homepage_body" | grep -qi "application error\|internal server error\|something went wrong\|error occurred\|this page could not be found"; then
            echo "❌ Canary failed: Homepage contains error message"
            echo "canary_status=failed_homepage" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          echo "✅ Homepage renders correctly"

          # Verify signup still renders because this is the top conversion entrypoint.
          echo "Checking signup route renders..."
          signup_url="${deployment_url%/}/signup"
          signup_response=$(curl -s -L -A "$USER_AGENT" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${signup_url}$( [[ "$signup_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
          signup_code="${signup_response##*$'\n'}"
          signup_body="${signup_response%$'\n'*}"

          if [ "$signup_code" != "200" ]; then
            echo "❌ Canary failed: /signup returned HTTP $signup_code"
            echo "canary_status=failed_signup" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ ${#signup_body} -lt 500 ]; then
            echo "❌ Canary failed: /signup body too short (${#signup_body} chars) - likely broken"
            echo "canary_status=failed_signup" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if echo "$signup_body" | grep -qi "application error\|internal server error\|something went wrong\|error occurred\|this page could not be found"; then
            echo "❌ Canary failed: /signup contains error message"
            echo "canary_status=failed_signup" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          echo "✅ Signup renders correctly"

          # Verify signin still renders because staging auth must stay healthy.
          echo "Checking signin route renders..."
          signin_url="${deployment_url%/}/signin"
          signin_response=$(curl -s -L -A "$USER_AGENT" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${signin_url}$( [[ "$signin_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
          signin_code="${signin_response##*$'\n'}"
          signin_body="${signin_response%$'\n'*}"

          if [ "$signin_code" != "200" ]; then
            echo "❌ Canary failed: /signin returned HTTP $signin_code"
            echo "canary_status=failed_signin" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ ${#signin_body} -lt 500 ]; then
            echo "❌ Canary failed: /signin body too short (${#signin_body} chars) - likely broken"
            echo "canary_status=failed_signin" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if echo "$signin_body" | grep -qi "application error\|internal server error\|something went wrong\|error occurred\|this page could not be found"; then
            echo "❌ Canary failed: /signin contains error message"
            echo "canary_status=failed_signin" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          echo "✅ Signin renders correctly"

          # Verify /api/chat is reachable and auth-gated. We send an unauthenticated
          # POST and expect HTTP 401 — that proves the route exists, the auth wall
          # is intact, and the handler is not 5xxing on boot (bad Sentry config,
          # missing env, broken import). A full streaming smoke would require auth
          # and is deferred (see chat instrumentation plan, 30-day review).
          echo "Checking /api/chat is reachable and auth-gated..."
          chat_url="${deployment_url%/}/api/chat"
          chat_response=$(curl -s -L -A "$USER_AGENT" -X POST -H "Content-Type: application/json" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -d '{"messages":[{"id":"1","role":"user","parts":[{"type":"text","text":"hi"}]}]}' -w "\n%{http_code}" "${chat_url}" || printf '\n000')
          chat_code="${chat_response##*$'\n'}"

          # 401 = expected (auth-gated). 400 = acceptable (validation ran, route alive).
          # Anything 5xx or 404 = regression.
          if [ "$chat_code" = "401" ] || [ "$chat_code" = "400" ]; then
            echo "✅ /api/chat reachable and auth-gated (HTTP $chat_code)"
          elif [ "$chat_code" = "404" ]; then
            echo "❌ Canary failed: /api/chat returned HTTP 404 — route missing or broken build"
            echo "canary_status=failed_chat_route" >> "$GITHUB_OUTPUT"
            exit 1
          elif [ "$chat_code" -ge 500 ] 2>/dev/null; then
            echo "❌ Canary failed: /api/chat returned HTTP $chat_code — handler crashing on boot"
            echo "canary_status=failed_chat_route" >> "$GITHUB_OUTPUT"
            exit 1
          else
            echo "⚠️ /api/chat returned unexpected HTTP $chat_code — investigate (non-blocking)"
          fi

          # Verify the public profile route renders without a server error.
          # "Profile not found" with HTTP 200 is a valid app response (data issue,
          # not a code regression), so only block on 5xx or actual error pages.
          echo "Checking public profile route renders..."
          profile_url="${deployment_url%/}/tim"
          profile_response=$(curl -s -L -A "$USER_AGENT" -H "Cache-Control: no-cache, no-store, must-revalidate" -H "Pragma: no-cache" "${BYPASS_ARGS[@]}" -w "\n%{http_code}" "${profile_url}$( [[ "$profile_url" == *"?"* ]] && echo '&' || echo '?' )_cb=$(date +%s%N)" || echo "\n000")
          profile_code="${profile_response##*$'\n'}"
          profile_body="${profile_response%$'\n'*}"

          if [ "$profile_code" -ge 500 ] 2>/dev/null; then
            echo "❌ Canary failed: /tim returned HTTP $profile_code (server error)"
            echo "canary_status=failed_public_profile" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if [ ${#profile_body} -lt 500 ]; then
            echo "❌ Canary failed: /tim body too short (${#profile_body} chars) - likely broken"
            echo "canary_status=failed_public_profile" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if echo "$profile_body" | grep -qi "application error\|internal server error\|something went wrong\|error occurred"; then
            echo "❌ Canary failed: /tim contains error content"
            echo "canary_status=failed_public_profile" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          if echo "$profile_body" | grep -qi "profile not found\|page could not be found"; then
            echo "⚠️ /tim shows 'not found' — profile may not be seeded in production (non-blocking)"
          else
            echo "✅ /tim profile renders with content"
          fi

          echo "✅ Canary passed: Deployment healthy and homepage renders"
          echo "canary_status=verified" >> "$GITHUB_OUTPUT"
