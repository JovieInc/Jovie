name: GitHub Copilot PR Review

on:
  # Disabled - github/copilot-pr-reviewer@v1 action does not exist
  # This workflow is temporarily disabled until a valid Copilot PR review action is available
  workflow_dispatch:
    inputs:
      dummy:
        description: 'Disabled workflow - no inputs needed'
        required: false

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  copilot-review:
    name: GitHub Copilot Review (Disabled)
    runs-on: ubuntu-latest
    
    steps:
      - name: Workflow Disabled
        run: |
          echo "This workflow is disabled because github/copilot-pr-reviewer@v1 action does not exist."
          echo "To re-enable, replace with a valid GitHub action for PR review."
          echo "Skipping PR review for now."
