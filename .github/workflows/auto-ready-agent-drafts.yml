name: Auto-Ready Agent Drafts

# Compatibility entrypoint only. An old proof cannot revive withdrawn readiness.
# The completing writer owns scripts/writer-owned-pr-promote.sh.
on:
  workflow_dispatch:
permissions: {}
jobs:
  auto-ready:
    name: Owner completion required
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Report retired fleet promotion
        run: echo 'owner-completion-required; fleet readiness recovery is retired; no action taken'
