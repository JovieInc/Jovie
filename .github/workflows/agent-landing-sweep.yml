# Retired: broad success sweeps are not owner completion intent.
name: Agent Landing Sweep
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          echo 'Retired. Owning agent must request exact-head native intent with pnpm ship:merge --repo OWNER/REPO --pr NUMBER --head SHA.'
          exit 2
