# Retired: broad success sweeps are not owner completion intent.
name: Agent Landing Sweep
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          echo 'Retired. The qualified owning agent may request native intent with node scripts/native-merge-intent.mjs --repo OWNER/REPO --pr NUMBER --head SHA.'
          exit 2
