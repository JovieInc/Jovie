#!/usr/bin/env bash
set -euo pipefail
set -a
if [[ -f "$HOME/.config/symphony/linear.env" ]]; then
  # shellcheck disable=SC1091
  . "$HOME/.config/symphony/linear.env"
fi
set +a
exec python3 "$HOME/.local/bin/symphony-codex-exhausted.py" reconcile "$@"
