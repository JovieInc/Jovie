---
type: concept
title: Jov 2193
ingested_via: put_page
ingested_at: '2026-05-31T12:52:33.340Z'
source_kind: put_page
---

# JOV-2193: Candidate follow-up: Bump vendored gstack from v0.14.5.0 to upstream HEAD

- **Status:** Backlog
- **Priority:** 🟡 Medium
- **Assignee:** Tim White
- **Team:** JOV
- **Project:** —
- **Labels:** codex
- **URL:** https://linear.app/jovie/issue/JOV-2193/candidate-follow-up-bump-vendored-gstack-from-v01450-to-upstream-head
- **Updated:** 2026-05-31T04:58:34.246Z

## Source

* Current issue: ad-hoc
* Source PR: (will follow once upstream gstack PRs land)
* Source branch/session: itstimwhite/investigate-ctatedev-tweet, investigation of vercel-labs/agent-browser

## Follow-up

The vendored gstack at `.agents/skills/gstack/` is at v0.14.5.0. Upstream `garrytan/gstack` is at v1.34.1.0 (May 2026). That's \~20+ minor versions and \~6 months of upstream work missing locally, including:

* `gstack-update-check` SHA-pinned VERSION fetch + semver-order guard
* Submodule consumability with exported server API + `AUTH_TOKEN` env sanitization
* Conductor worktree leak guard in `./setup`
* Long-skill mid-flow learnings refresh (`/investigate`, `/qa`, `/ship`)
* `/sync-gbrain` rewrite (batch import via `gbrain import <dir>`, opt-in `--scan-secrets`, SIGTERM-safe shutdown)
* Many new commands, skills, and resolver upgrades not enumerated here

Plan: clone `garrytan/gstack` at current HEAD into `.agents/skills/gstack/`, regenerate skill docs, rebuild Bun binaries, run `pnpm --filter @jovie/web run typecheck` + Biome + post-task-validate hook. Adjust any Jovie CLAUDE.md or rules references that point at removed skills.

## Why it matters

* Six months of upstream fixes and features unavailable locally
* Frozen vendored copy diverges further every week, increasing migration risk
* Some Jovie rules already reference upstream skills (e.g. `gbrain` MCP guidance) that assume newer behavior

## Classification

Candidate

## Acceptance criteria or triage question

Pickup agent must first judge whether to implement, close, or split this. If implementing:

- [ ] Vendor `garrytan/gstack` at current HEAD into `.agents/skills/gstack/`
- [ ] Run `cd .agents/skills/gstack && ./setup` to rebuild binaries
- [ ] Run `pnpm --filter @jovie/web run typecheck -- --pretty false`, `pnpm biome check apps/web`, and the post-task-validate hook
- [ ] Verify `/browse`, `/qa`, `/ship`, `/review`, `/investigate` still resolve (no `command not found`)
- [ ] Skim CHANGELOG entries v0.15 → v1.34 for any breaking change that needs a Jovie-side reaction (auth, hooks, skill names, env vars)
- [ ] Update `.claude/rules/gstack.md` if skill names changed
- [ ] One PR with the full diff; CHANGELOG note pointing at the upstream version landed

## Dependency

Depends on upstream PRs landing first (browse `record` command + `/qa --evidence-per-finding` flag). Pickup agent should wait until both upstream PRs merge so the vendored bump pulls in our own contributions in the same sweep.

Pickup agent must first judge whether to implement, close, or split this.


## Comments

### Unknown (2026-05-14)
This comment thread is synced to a corresponding [GitHub issue](https://github.com/JovieInc/Jovie/issues/8649). All replies are displayed in both locations.
