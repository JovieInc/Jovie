{
  "thresholds": {
    "totalDuration": 120000,
    "p95": 1000,
    "setupTime": 75000,
    "individualTest": 1000,
    "slowTestCount": 25
  },
  "budgets": {
    "smoke": {
      "maxTotalDuration": 30000,
      "description": "Smoke suite should finish under 30s to keep deploy feedback instant."
    },
    "critical": {
      "maxTotalDuration": 400000,
      "description": "Critical regression suite (includes auth E2E tests)"
    },
    "full": {
      "maxTotalDuration": 300000,
      "description": "Full coverage runs should stay under 5 minutes to guard against drift."
    }
  }
}
