# E2E Test Environment Variables
# Copy this to .env.local or set in your CI environment

# ===== Required for All E2E Tests =====
# Base URL for testing (defaults to http://localhost:3000)
BASE_URL=http://localhost:3000

# ===== Required for Authentication Tests =====
# Clerk authentication (get from https://dashboard.clerk.com)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
CLERK_SECRET_KEY=sk_test_your_secret_here

# ===== Required for Full Onboarding Tests =====
# Enable full onboarding test suite (requires real auth)
E2E_ONBOARDING_FULL=1

# Optional: Custom test user credentials
E2E_TEST_EMAIL=playwright-test@example.com
E2E_TEST_PASSWORD=TestPassword123!

# ===== Required for Existing User Tests =====
# Credentials for an existing user in your test database
E2E_CLERK_USER_USERNAME=testuser@example.com
E2E_CLERK_USER_PASSWORD=ExistingPassword123!

# ===== Required for RLS Testing (Access Control) =====
# Neon database connection string (for RLS verification)
DATABASE_URL=postgresql://user:password@ep-preview-xyz.region.aws.neon.tech/neondb?sslmode=require

# ===== Optional Feature Flags =====
# Enable specific test suites
E2E_ONBOARDING_HANDLE_TAKEN=1  # Test handle availability checks
E2E_SYNTHETIC_MODE=true        # Run synthetic monitoring tests

# Test environment (used for conditional test behavior)
E2E_ENVIRONMENT=development    # development | preview | production

# ===== Optional Test Configuration =====
# Test profile handle (for golden path tests)
E2E_TEST_PROFILE=testartist

# ===== Notes =====
# 1. For local development, most tests run with mocked data
# 2. For CI/CD, set E2E_ONBOARDING_FULL=1 to run full suite
# 3. Preview deployments should use preview Clerk instance
# 4. Production monitoring uses production Clerk instance
# 5. Never commit real credentials to version control
