{
  "name": "@jovie/desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "tsc && electron .",
    "build:staging": "cross-env ELECTRON_ENV=staging tsc && electron-builder --config electron-builder.yml --config.appId=app.jov.ie.staging --config.mac.icon=assets/icon-staging.icns --publish always",
    "build:production": "cross-env ELECTRON_ENV=production tsc && electron-builder --config electron-builder.yml --publish always",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "cross-env": "^7.0.3",
    "electron": "^39.8.5",
    "electron-builder": "^25.0.0",
    "sharp": "^0.33.0",
    "typescript": "^5.7.0"
  }
}
