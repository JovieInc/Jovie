import { AuthLoader } from '@/components/organisms/AuthLoader';

/**
 * Artist selection page loading screen
 * Uses AuthLoader to match the unified layout
 */
export default function ArtistSelectionLoading() {
  return <AuthLoader />;
}
