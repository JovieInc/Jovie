/**
 * Database Schema
 *
 * @deprecated Import from '@/lib/db/schema' (the schema/ directory) instead.
 * This file re-exports all schemas for backwards compatibility.
 * New code should import directly from the modular schema files.
 *
 * @example
 * // Preferred (new code):
 * import { users } from '@/lib/db/schema/auth';
 *
 * // Also works (backwards compatible):
 * import { users } from '@/lib/db/schema/auth';
 */

export * from './schema/index';
