export interface ContributionData {
  readonly date: string;
  readonly count: number;
  readonly level: number;
  readonly accentColor?: string;
  readonly accentMuted?: boolean;
  readonly label?: string;
}
