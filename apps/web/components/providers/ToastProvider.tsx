/**
 * @deprecated Import `FeedbackProvider` from `@/components/feedback`
 * instead. This compatibility re-export keeps older call sites working
 * while they migrate to the canonical feedback system.
 */
export { FeedbackProvider as ToastProvider } from '@/components/feedback';
