# Cursor Rules for Jovie Project

## ⚠️ CRITICAL: BRANCH PROTECTION RULES

### **NEVER PUSH TO PREVIEW OR MAIN**

- **ONLY push to `develop` branch**
- **NEVER push directly to `preview` or `main` branches**
- The CI/CD pipeline handles all promotions automatically
- If pipeline is stuck, fix issues on `develop` and let CI handle the rest
- Direct pushes to protected branches will be rejected and can break the pipeline

### **Branch Protection**

- `preview` and `main` are protected branches
- All changes must go through the CI/CD pipeline
- Auto-promote workflows handle `develop → preview → main` progression
- Manual intervention should only be done on `develop` branch

## Project Overview

Jovie is a Next.js application with Better Auth and Neon PostgreSQL. Clerk is retired — do not import `@clerk/*`.

## Technology Stack

- **Frontend**: Next.js 14 (App Router)
- **Database**: Supabase (PostgreSQL with RLS)
- **Authentication**: Better Auth (self-hosted)
- **Styling**: Tailwind CSS
- **Testing**: Vitest + Playwright
- **Deployment**: Vercel

## Critical Guidelines

### 1. Authentication (Better Auth)

**ALWAYS use Better Auth.** Canonical rules: `.claude/rules/auth.md`.

```typescript
// Server
import { getCachedAuth } from '@/lib/auth/cached';
const { userId } = await getCachedAuth();

// Client
import { useJovieAuth } from '@/hooks/useJovieAuth';
const { user } = useJovieAuth();
```

**NEVER** import `@clerk/*`, add `CLERK_*` env, or create Clerk test users.

### 2. RLS Policies

**ALWAYS use auth.jwt() for user identification:**

```sql
-- ✅ CORRECT: Use auth.jwt() for RLS
create policy "User can view own data" on "users"
for select to authenticated using (
  auth.jwt()->>'sub' = user_id
);
```

### 3. Component Patterns

**Use the established patterns:**

```typescript
// ✅ CORRECT: Client component with Better Auth
'use client';
import { useJovieAuth } from '@/hooks/useJovieAuth';

export function MyComponent() {
  const { user } = useJovieAuth();
  void user;
}
```

### 4. Error Handling

**Always handle errors gracefully:**

```typescript
const { data, error } = await supabase.from('table').select('*');

if (error) {
  console.error('Database error:', error);
  // Handle error appropriately
  return;
}
```

### 5. TypeScript

**Use proper typing:**

```typescript
interface User {
  id: string;
  clerk_id: string;
  email: string;
  created_at: string;
}

const { data: users, error } = await supabase
  .from('users')
  .select('*')
  .returns<User[]>();
```

## File Structure

- `app/` - Next.js App Router pages and layouts
- `components/` - Reusable React components
- `lib/` - Utility functions and configurations
- `supabase/` - Database migrations and functions
- `types/` - TypeScript type definitions
- `tests/` - Test files

## Environment Variables

Required environment variables:

```bash
# Better Auth
BETTER_AUTH_SECRET=...
BETTER_AUTH_URL=http://localhost:3100

# Neon
DATABASE_URL=postgresql://...

# Spotify
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret
```

## Database Schema

Key tables:

- `users` - User accounts (`better_auth_user_id` links to `ba_users`)
- `artists` - Artist profiles
- `social_links` - Social media links
- `releases` - Music releases
- `click_events` - Analytics data

## Testing Guidelines

1. **Unit Tests**: Use Vitest for component testing
2. **E2E Tests**: Use Playwright for end-to-end testing
3. **Database Tests**: Test RLS policies with multiple users
4. **Authentication Tests**: Use `E2E_USE_TEST_AUTH_BYPASS=1` and `/api/dev/test-auth/enter`

## Code Style

- Use TypeScript for all new code
- Follow existing component patterns
- Use Tailwind CSS for styling
- Prefer functional components with hooks
- Use proper error boundaries and loading states

## Security Guidelines

1. **Always enable RLS** on user data tables
2. **Use auth.jwt()** for user identification in policies
3. **Validate user permissions** before database operations
4. **Never expose sensitive data** in client-side code
5. **Use environment variables** for all secrets

## Auth Guidelines

When changing auth:

1. **Keep Better Auth as the only runtime** — no Clerk SDK or `CLERK_*` env
2. **Use `users.better_auth_user_id` / `users.id`** in new code
3. **Leave `users.clerk_id` nullable** for historical rows only
4. **Test with the Better Auth bypass** (`E2E_USE_TEST_AUTH_BYPASS=1`)

## Common Patterns

### Form Handling

```typescript
const [loading, setLoading] = useState(false);
const [error, setError] = useState<string | undefined>();

const handleSubmit = async (e: React.FormEvent) => {
  e.preventDefault();
  setLoading(true);
  setError(undefined);

  try {
    const { userId } = await getCachedAuth();
    if (!userId) throw new Error('Unauthorized');

    if (error) throw error;
    // Handle success
  } catch (error) {
    setError('Failed to save data');
  } finally {
    setLoading(false);
  }
};
```

### Data Fetching

```typescript
const [data, setData] = useState<User[]>([]);
const [loading, setLoading] = useState(true);

useEffect(() => {
  const fetchData = async () => {
    try {
      const { userId } = await getCachedAuth();
      void userId;

      if (error) throw error;
      setData(data || []);
    } catch (error) {
      console.error('Error fetching data:', error);
    } finally {
      setLoading(false);
    }
  };

  fetchData();
}, []);
```

## Resources

- [Better Auth](https://www.better-auth.com/docs)
- `.claude/rules/auth.md`
- [Next.js App Router](https://nextjs.org/docs/app)
- [Tailwind CSS](https://tailwindcss.com/docs)

## Infrastructure & Cost Guardrails (CRITICAL)

**Before creating any cron job, scheduled task, or polling loop:**

1. **Webhook first** — Can an external service notify you? (Stripe, Resend all have webhooks)
2. **Inline after-action** — Can it happen right after the triggering action?
3. **Lazy evaluation** — Can it happen when data is next accessed?
4. **Add to existing job** — Can you extend an existing cron?
5. **New job (last resort)** — Only with explicit human approval and PR justification.

**NEVER:**
- Create a new Vercel cron entry without human approval
- Iterate over all users to call an external API (24,000+ calls/day at 1k users hourly)
- Poll external APIs for state available via webhook
- Add job queue libraries (Bull, Agenda, etc.)
- Use `setInterval`/`setTimeout` in server code (serverless functions terminate)

**ALWAYS** include a Cost Impact section in PRs with recurring external API calls.

See `AGENTS.md` at repo root for full infrastructure guardrails.

---

## AI Tool Verification

Before suggesting code changes, verify:

1. ✅ Uses Better Auth (`useJovieAuth` / `getCachedAuth`)
2. ✅ Implements proper RLS policies
3. ✅ Handles errors gracefully
4. ✅ Uses TypeScript types
5. ✅ Follows existing patterns
6. ✅ Maintains security best practices
7. ✅ No unnecessary cron jobs or scheduled tasks (see guardrails above)
8. ✅ No excessive external API calls in loops
