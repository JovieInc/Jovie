// Authored preview — HomepageV2Pricing. Homepage pricing plans section.
// No props — the section is self-contained; one canonical story.
import { HomepageV2Pricing } from 'apps/web/components';

export function Default() {
  return <HomepageV2Pricing />;
}
