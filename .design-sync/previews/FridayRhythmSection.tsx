// Authored preview — FridayRhythmSection. Friday release rhythm contribution-graph section.
// No props — the section is self-contained; one canonical story.
import { FridayRhythmSection } from 'apps/web/components';

export function Default() {
  return <FridayRhythmSection />;
}
