// Authored preview — HomeLoopDiagramSection. The release loop diagram section.
// No props — the section is self-contained; one canonical story.
import { HomeLoopDiagramSection } from 'apps/web/components';

export function Default() {
  return <HomeLoopDiagramSection />;
}
