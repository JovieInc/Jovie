// Authored preview — HomeComposerHero. Animated AI composer demo (self-running).
// No props — the section is self-contained; one canonical story.
import { HomeComposerHero } from 'apps/web/components';

export function Default() {
  return <HomeComposerHero />;
}
