// Authored preview — HomepageV2FinalCta. Homepage final call-to-action section.
// No props — the section is self-contained; one canonical story.
import { HomepageV2FinalCta } from 'apps/web/components';

export function Default() {
  return <HomepageV2FinalCta />;
}
