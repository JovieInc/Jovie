/**
 * Shortcut Scope Hook
 * 
 * Re-export hook from the provider for easier imports
 */

export { 
  useShortcutScope,
  useScopedShortcuts,
  useModalShortcuts,
  useSheetShortcuts,
  useCurrentScope
} from '@/components/providers/ShortcutScope';