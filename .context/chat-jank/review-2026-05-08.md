# Chat Jank Monitor — 2-week review (2026-05-08)

## Outcome: gate never enabled — zero exposure

- Gate `chat_jank_monitor` default: `false` (unchanged in `APP_FLAG_DEFAULTS`)
- Sentry search across `errors`, `logs`, `spans` datasets for all 8 event names: **0 results**
- No threshold changes made

## Action required

Enable the `chat_jank_monitor` gate in Statsig (start at 10–20% of users), then re-run review in 2 weeks.

## Next review target

Branch: `chat-jank/tuning-2026-05-22`
